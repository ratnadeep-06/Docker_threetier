services:
    boxer:
     build: .
     ports:
       - '80:80'
     container_name: boxercontainer
     volumes: 
       - myapp:/var/www/html/
     network_mode:
       bridge

volumes: 
    myapp:         
     
